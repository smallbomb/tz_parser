# tz_parser
```
usage: tz_parser [ zonename | -p <absolute path of zonename>] 
  -h               : help
  -p [abs path]    : absolute path.
Example:
  tz_parser Asia/Taipei
  tz_parser -p /usr/share/zoneinfo/Asia/Taipei
```